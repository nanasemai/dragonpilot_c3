# NVIDIA GT640M 工程编译和验证计划

## 目标
编译dragonpilot_pc工程，确认环境变量正确设置，GPU正确识别，系统能正常运行。

## 执行步骤

### 第一阶段：环境准备和验证
1. **检查环境变量配置**
   - 读取并验证`.env`文件中的CL_HALF、CL_INT64、CL_OPTIMIZATION_LEVEL设置
   - 确认DEV=GPU、IMAGE=0等必要设置

2. **GPU识别测试**
   - 运行GPU识别脚本，确认GT640M被正确识别
   - 验证FP16、Int64、Double支持状态
   - 确认自动检测机制工作正常

### 第二阶段：工程编译
3. **编译dragonpilot工程**
   - 查找项目的编译命令或脚本
   - 运行模型编译测试
   - 验证OpenCL编译成功

### 第三阶段：系统运行验证
4. **运行系统测试**
   - 执行实际的程序测试
   - 监控GPU使用情况
   - 验证系统稳定运行

### 第四阶段：输出验证报告
5. **生成验证报告**
   - 环境变量配置状态
   - GPU识别结果
   - 编译成功率
   - 系统运行状态

## 预期结果
- ✅ 环境变量全部正确设置（CL_HALF=0, CL_INT64=0, CL_OPTIMIZATION_LEVEL=0）
- ✅ GPU正确识别为NVIDIA GT640M
- ✅ OpenCL编译成功
- ✅ 系统能够正常运行

## 详细命令

```bash
# 1. 加载环境变量
cd /home/ubuntu/dragonpilot_pc
source .env

# 2. 验证环境变量
env | grep -E "CL_(HALF|INT64|OPTIMIZATION)" 

# 3. 运行GPU识别测试
PYTHONPATH=. DEBUG=1 python3 test_gt640m_compat.py

# 4. 编译工程
python examples/openpilot/compile3.py selfdrive/modeld/models/driving_policy.onnx

# 5. 运行系统测试
# 根据实际项目结构运行相应测试
```

开始执行此计划。